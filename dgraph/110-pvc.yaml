---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dgraph-alpha-pvc
  namespace: dgraph
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
