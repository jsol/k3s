---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loopia-pvc
  namespace: loopia
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1M
